<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a  class="text-sm bg-red-500 rounded-md px-3 py-2 mb-5 inline-block" href="/logout">Logout</a>
        </div>
        
        <h5 class="mb-5">edit your post</h5>
        <form  method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind?" class=" p-3 w-1/3 bg-transparent border-2 border-zinc-800 rounded-md outline-none resize-none" name="content"><%= post.content %></textarea>
            <input type="submit" value="Update post" class="px-3 py-2  text-sm w-40 bg-yellow-500  text-black rounded-md outline-none block mt-2" />
        </form>
    </div>
</body>
</html>